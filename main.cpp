#include <iostream>
#include <fstream>
#include "Command.h"
#include "unordered_map"
#include "vector"



int main(int argc, char** argv) {
  //create map here

  return 0;
}

vector<string> lexer (FILE* f) {
  // create array of strings
}

void parser (vector<string> array, unordered_map<string, Command> map) {
  // parse vector and execute through map
}